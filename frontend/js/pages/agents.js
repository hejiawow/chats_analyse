/* ============================================
   Agents Page — 智能体管理
   ============================================ */

// The agents page is mostly static HTML with badge counts.
// If dynamic data loading is needed, it can be added here.

function loadAgentsPage() {
  // Currently the agents page displays static info cards
  // No dynamic data loading required
}
